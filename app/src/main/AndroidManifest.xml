<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exoest.mytesting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation"
            android:icon="@mipmap/ic_launcher"
            android:label="The First Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DogActivity"
            android:label="Dog Page"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CatActivity"
            android:label="Cat Page"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RetrievedDetailsActivity"
            android:label="Details Confirmation"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>